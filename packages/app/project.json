{
  "name": "@tb/app",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bun run vite build",
        "cwd": "{projectRoot}"
      }
    },
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bun run vite",
        "cwd": "{projectRoot}"
      }
    }
  }
}